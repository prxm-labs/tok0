[filter]
name = "trunk-build"
commands = ["trunk build"]
strip_patterns = [
  "^\\s*$",
  "^\\s*Compiling\\s",
  "^\\s*Downloading\\s",
  "^\\s*Fetching\\s",
  "^\\s*Fresh\\s",
  "^\\s*Checking\\s",
]
tail_lines = 5
head_lines = 20
empty_message = "trunk build: ok"
