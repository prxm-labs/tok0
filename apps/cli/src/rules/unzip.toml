[filter]
name = "unzip"
commands = ["unzip"]
strip_patterns = [
  "^\\s*$",
  "^\\s*inflating: ",
  "^\\s*extracting: ",
  "^\\s*creating: ",
  "^Archive:  ",
]
head_lines = 20
tail_lines = 5
max_line_chars = 140
