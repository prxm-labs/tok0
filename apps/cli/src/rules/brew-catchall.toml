[filter]
name = "brew-catchall"
commands = ["brew"]
strip_patterns = [
  "^\\s*$",
  "^==> Downloading",
  "^==> Pouring",
  "^Already downloaded:",
  "^==> Fetching",
  "^###",
  "^==> Auto-updated Homebrew",
  "^Updated \\d+ taps?",
  "^==> Checking for",
]
head_lines = 30
tail_lines = 10
max_line_chars = 200
empty_message = "brew: ok"
