[filter]
name = "redis-catchall"
commands = ["redis-cli"]
strip_patterns = [
  "^\\s*$",
  "^\\s*#\\s+(Server|Clients|Memory|Persistence|Stats|Replication|CPU|Modules|Keyspace|Errorstats|Latencystats|Debug)",
]
head_lines = 20
tail_lines = 5
max_line_chars = 140
